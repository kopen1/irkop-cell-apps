# Irkop Cell Native

Fresh Android-native rebuild of Irkop Cell.

The UI is implemented from the supplied Stitch ZIP as the visual source of truth. Previous Android migration source code is not used as the UI foundation.

Current branch: `main`.
